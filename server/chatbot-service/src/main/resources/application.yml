server:
  port: 8087

spring:
  application:
    name: chatbot-service
  
  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:${DB_PORT:3306}/kayak_chatbot_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password}
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

  # Flyway Migrations
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

  # DevTools
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
    livereload:
      enabled: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:your-super-secret-jwt-key-here-change-in-production}

# Chatbot Configuration
chatbot:
  openrouter-token: ${OPENROUTER_TOKEN:}
  hf-token: ${HF_TOKEN:}

# gRPC Client Targets
grpc:
  client:
    route-service:
      host: localhost
      port: 9093
    schedule-service:
      host: localhost
      port: 9094
    fleet-service:
      host: localhost
      port: 9092

# Logging
logging:
  level:
    root: INFO
    com.example.chatbot: DEBUG
    org.springframework.security: DEBUG
